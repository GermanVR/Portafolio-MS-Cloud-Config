server:
  port: 9100
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
config:
  security:
    oauth:
      client:
        id: frontendapp
        secret: 12345
    jwt:
      key: algo
